

spring:
  application:
    name: client-test3

#服务端口
server:
  port: 8092

# 注册中心地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7890/eureka/
  instance:
    prefer-ip-address: true
